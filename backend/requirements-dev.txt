# =============================================================================
#  requirements-dev.txt  —  Local development (GPU-capable wheels)
#
#  Install with:   pip install -r requirements-dev.txt
#
#  Pulls full tensorflow + torch + torchvision + ultralytics. On a machine
#  with a working CUDA toolkit + drivers, these auto-use the GPU. Without
#  GPU, they fall back to CPU automatically (just larger downloads/RAM).
#
#  For production / Docker / Vercel use requirements.txt (CPU-only) instead.
# =============================================================================


-r requirements-base.txt


# --- Deep Learning — TensorFlow / Keras (GPU build) -------------------------
tensorflow>=2.15,<3
keras>=3.0,<4


# --- Deep Learning — PyTorch (default index; will pull CUDA wheels on linux/win) ---
torch>=2.3,<3
torchvision>=0.18,<1


# --- Object Detection — Ultralytics / YOLO ----------------------------------
ultralytics>=8.0,<9


# --- Testing (pytest + in-memory Mongo + fake Redis) ------------------------
#  Dev/CI only — not installed in production images. `mongomock` gives an
#  in-memory MongoDB and `fakeredis` an in-memory Redis, so the test-suite runs
#  with no real MongoDB / Redis servers.
pytest>=8.0,<9
mongomock>=4.1,<5
fakeredis>=2.20,<3
