# =============================================================================
#  requirements.txt  —  Production / Docker / Vercel (CPU-only)
#
#  This is the default for Dockerfile (see ARG REQUIREMENTS_FILE) and for any
#  deployment target where GPU is unavailable. Builds ~1.3 GB images.
#
#  For local development with GPU support, use:   pip install -r requirements-dev.txt
#  For RAM-constrained free-tier deploys (no DL),  use: -r requirements.railway.txt
# =============================================================================


-r requirements-base.txt


# --- Deep Learning — TensorFlow / Keras (CPU-only build) --------------------
tensorflow-cpu>=2.15,<3
keras>=3.0,<4


# --- Deep Learning — PyTorch (CPU wheels live on a separate index) ----------
#  IMPORTANT: the --extra-index-url MUST appear in this file (not in -r'd
#  files) so pip picks up the +cpu wheels at resolution time.
--extra-index-url https://download.pytorch.org/whl/cpu

torch==2.3.1+cpu
torchvision==0.18.1+cpu


# --- Object Detection — Ultralytics / YOLO ----------------------------------
ultralytics>=8.0,<9
