[pytest]
# Run from the backend/ directory: `python -m pytest`
# pythonpath=. puts backend/ on sys.path so tests can `from config import ...`,
# `from app import create_app`, etc. — matching how the app imports itself.
pythonpath = .
testpaths = tests
addopts = -q
filterwarnings =
    ignore::DeprecationWarning
